global:
  cloudProvider: "google"

  google:
    hostname: ""
    gateway:
      name: google-gateway-internal
      namespace: google-gateway

kiali-server:
  deployment:
    ingress:
      enabled: false
