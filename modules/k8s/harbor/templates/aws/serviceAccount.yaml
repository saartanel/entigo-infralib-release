#{{- if eq .Values.cloudProvider "aws" }}

#apiVersion: v1
#kind: ServiceAccount
#metadata:
  #annotations:
    #eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.account }}:role/{{ .Release.Name }}
  #name: harbor

#{{- end }}
