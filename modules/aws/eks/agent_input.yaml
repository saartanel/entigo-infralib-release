vpc_id: "{{ .toutput.vpc.vpc_id }}"
private_subnets: |
  [{{ .toutput.vpc.private_subnets }}]
public_subnets: |
  [{{ .toutput.vpc.public_subnets }}]
eks_api_access_cidrs: |
  [{{ .toutput.vpc.private_subnet_cidrs }}]
cloudwatch_log_group_kms_key_id: "{{ .toutput.kms.telemetry_alias_arn }}"
cluster_encryption_kms_key_arn: "{{ .toutput.kms.config_alias_arn }}"
node_encryption_kms_key_arn: "{{ .toutput.kms.data_alias_arn }}"
