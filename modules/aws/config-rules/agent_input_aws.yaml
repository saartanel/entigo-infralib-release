config_logs_bucket: "{{ .config.prefix }}-{{ .module.name }}-{{ .agent.accountId }}-{{ .agent.region }}-config-logs"
cloudtrail_logs_bucket: "{{ .config.prefix }}-{{ .module.name }}-{{ .agent.accountId }}-{{ .agent.region }}-cloudtrail-logs"
