ip_range_pods: "{{ .toutput.vpc.private_subnets_pods[0] }}"
ip_range_services: "{{ .toutput.vpc.private_subnets_services[0] }}"
subnetwork: "{{ .toutput.vpc.private_subnets[0] }}"
network: "{{ .toutput.vpc.vpc_name }}"
